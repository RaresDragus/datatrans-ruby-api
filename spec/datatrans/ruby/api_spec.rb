RSpec.describe Datatrans::Ruby::Api do
  it "has a version number" do
    expect(Datatrans::Ruby::Api::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
